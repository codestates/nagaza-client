import React from "react";
import axios from "axios";


export default function SignInErrorBox(props) {
  state = {
    targetData : typeof props.type,
  };

  return (
      {
         //1.입력이 이메일일 때
         //2.입력이 비밀번호일 때
      }
    
  );
}
