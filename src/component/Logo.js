import React from "react";

export default class Logo extends Component {
  constructor(props) {
    super(props);
  }
  //로고 hover event, click event = redirection to LandingPage
  render() {
    return <img className="logoImage" src="#"></img>;
  }
}
